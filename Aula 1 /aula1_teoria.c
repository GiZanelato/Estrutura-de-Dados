#include <stdio.h>

typedef struct{
    int values[LEN];
    int n;
}list;

int is_full(list *l){
   return l->n == LEN     quando usa return antes de uma omoa     
} 

int main(){
    printf("heloo world");
    return 0;
}